<h1>Listing my books</h1>

<table>
  <tr>
    <th>title</th>
    <th>author</th>

    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>

        <td><%= link_to 'Return',return_book_path(book) %></td>
        <td></td>
        <td></td>
      </tr>
  <% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>